---
title: "POST /api/v1/auth/token/"
description: "Emite o JWT usado nas demais chamadas."
openapi: "openapi/public.json POST /api-token-auth/"
---

<Note>
Contrato público: `POST /api/v1/auth/token/`. O playground chama a homologação GSF.
</Note>

Use o valor retornado no header:

```http
Authorization: JWT <token>
```
